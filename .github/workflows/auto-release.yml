name: Auto Release

on:
  push:
    branches:
      - main
    paths:
      - 'Launcher/resource/Launcher.rc'
      - 'Mod/resource/Mod.rc'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  auto-release:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6
      with:
        fetch-depth: 0

    - name: Extract version from RC files
      id: extract_version
      shell: pwsh
      run: |
        # Extract version from Launcher.rc
        $launcherRc = Get-Content "Launcher/resource/Launcher.rc" -Raw -Encoding UTF8
        if ($launcherRc -match 'FILEVERSION\s+(\d+),\s*(\d+),\s*(\d+),\s*(\d+)') {
          $launcherVersion = "v$($matches[1]).$($matches[2]).$($matches[3])"
          Write-Output "launcher_version=$launcherVersion" >> $env:GITHUB_OUTPUT
          Write-Host "Launcher version: $launcherVersion"
        }
        
        # Extract version from Mod.rc
        $modRc = Get-Content "Mod/resource/Mod.rc" -Raw -Encoding UTF8
        if ($modRc -match 'FILEVERSION\s+(\d+),\s*(\d+),\s*(\d+),\s*(\d+)') {
          $modVersion = "v$($matches[1]).$($matches[2]).$($matches[3])"
          Write-Output "mod_version=$modVersion" >> $env:GITHUB_OUTPUT
          Write-Host "Mod version: $modVersion"
        }
        
        # Validate versions were extracted
        if (-not $launcherVersion -and -not $modVersion) {
          throw "Failed to extract version from RC files"
        }

        # Determine which version to use (use the higher one)
        $finalVersion = $launcherVersion
        if ($modVersion -and $launcherVersion) {
          $versions = @($launcherVersion, $modVersion) | Where-Object { $_ }
          $finalVersion = ($versions | Sort-Object {[version]($_ -replace '^v', '')} | Select-Object -Last 1)
        } elseif ($modVersion) {
          $finalVersion = $modVersion
        }
        Write-Output "release_version=$finalVersion" >> $env:GITHUB_OUTPUT
        Write-Host "Final release version: $finalVersion"

    - name: Check if tag already exists
      id: check_tag
      shell: pwsh
      run: |
        $version = "${{ steps.extract_version.outputs.release_version }}"
        $existingTag = git tag -l $version
        if ($existingTag) {
          Write-Host "Tag $version already exists"
          Write-Output "tag_exists=true" >> $env:GITHUB_OUTPUT
        } else {
          Write-Host "Tag $version does not exist"
          Write-Output "tag_exists=false" >> $env:GITHUB_OUTPUT
        }

    - name: Setup MSBuild
      if: steps.check_tag.outputs.tag_exists == 'false'
      uses: microsoft/setup-msbuild@v2

    - name: Cache NuGet packages
      if: steps.check_tag.outputs.tag_exists == 'false'
      uses: actions/cache@v5
      with:
        path: ~/.nuget/packages
        key: nuget-${{ hashFiles('**/*.vcxproj') }}

    - name: Build Release
      if: steps.check_tag.outputs.tag_exists == 'false'
      shell: pwsh
      run: |
        MSBuild.exe "Half Sword Enhancer.sln" /p:Configuration=Release /p:Platform=x64
        if ($LASTEXITCODE -ne 0) { throw "Build failed" }

        New-Item -ItemType Directory -Force -Path "release_files" | Out-Null
        Copy-Item "bin/HSEnhancerLauncher.exe" "release_files/HSEnhancerLauncher.exe"
        Copy-Item "bin/HSEnhancer.dll" "release_files/HSEnhancer.dll"

        "### Build Complete ✅`nVersion: ${{ steps.extract_version.outputs.release_version }}" >> $env:GITHUB_STEP_SUMMARY

    - name: Create manual installation ZIP
      if: steps.check_tag.outputs.tag_exists == 'false'
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Force -Path "manual_install" | Out-Null

        Copy-Item "Manual_Install.txt" "manual_install/README.txt"

        Copy-Item "bin/dwmapi.dll" "manual_install/dwmapi.dll"
        Copy-Item "release_files/HSEnhancer.dll" "manual_install/HSEnhancer.dll"

        Compress-Archive -Path "manual_install/*" -DestinationPath "release_files/HSEnhancer.zip" -Force

    - name: Extract changelog for version
      if: steps.check_tag.outputs.tag_exists == 'false'
      id: changelog
      shell: pwsh
      run: |
        $version = "${{ steps.extract_version.outputs.release_version }}"
        
        $defaultContent = "Release $version - See commit history for details."
        
        if (Test-Path "CHANGELOG.md") {
          $content = Get-Content "CHANGELOG.md" -Raw
          $pattern = "(?s)## \[$([regex]::Escape($version))\].*?\n(.*?)(?=\n## |\z)"
          if ($content -match $pattern) {
            $changelogContent = $matches[1].Trim()
          } else {
            $changelogContent = $defaultContent
          }
        } else {
          $changelogContent = $defaultContent
        }
        
        Write-Output "changelog_content<<EOF" >> $env:GITHUB_OUTPUT
        Write-Output $changelogContent >> $env:GITHUB_OUTPUT
        Write-Output "EOF" >> $env:GITHUB_OUTPUT

    - name: Create GitHub Release
      if: steps.check_tag.outputs.tag_exists == 'false'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ steps.extract_version.outputs.release_version }}
        name: ${{ steps.extract_version.outputs.release_version }}
        body: ${{ steps.changelog.outputs.changelog_content }}
        draft: true
        prerelease: false
        files: |
          release_files/HSEnhancerLauncher.exe
          release_files/HSEnhancer.dll
          release_files/HSEnhancer.zip
        generate_release_notes: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}