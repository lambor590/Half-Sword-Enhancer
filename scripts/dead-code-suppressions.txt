# Functions that cppcheck reports as unused due to analysis limitations.
# All entries below have been manually verified.

# Launcher -- unused utility (pre-existing, out of scope for Mod rewrite)
ToCompactString

# Override system -- called by editor sections via override descriptor tables
CountActive
SerializeAll
DeserializeAll
RenderOverrideField
RenderOverrideGroup

# Preset system -- called from PresetSerializer<DataType> template (cppcheck can't see template instantiation)
SerializePresetFields
DeserializePresetFields
